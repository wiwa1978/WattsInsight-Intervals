{
  "name": "@platform/platform-db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "db:generate": "bun run --cwd ../.. db:generate",
    "db:migrate": "bun run --cwd ../.. db:migrate",
    "db:check": "bun run --cwd ../.. db:check",
    "test": "vitest run tests",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "postgres": "^3.4.9"
  }
}
